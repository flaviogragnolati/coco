interface:
  display_name: "Reporting Workflow"
  short_description: "Coordinate traceable reports and presentation channels"
  default_prompt: "Use $q-report-workflow to create an approved general or market-research report from versioned project artifacts and render the requested document, Marp, or PowerPoint channels."
